// 백준 2748 : 피보나치 수 2
#include <vector>
#include <string>
#include <iostream>

using namespace std;

long long dp[91]; // 사용할 인덱스 : 0~90

int main() {
	int n;
	cin >> n;

	dp[0] = 0;
	dp[1] = 1;

	for (int i = 2; i <= n; i++) {
		dp[i] = dp[i - 1] + dp[i - 2];
	}

	cout << dp[n] << endl;
}